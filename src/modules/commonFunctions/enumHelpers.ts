export const getEnumKeys = (en: any): Array<string> => {
  return Object.keys(en) as Array<string>;
}

export const getEnumValues = (en: any): Array<string> => {
  return Object.values(en) as Array<string>;
}
